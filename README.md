# Umwad Portal

Clone this repo and to the following:

## `npm install`

To run the project do:

## `npm start`

To create build package do:

## `npm run build`

For firebase deployment install first the `firebase-tools` by doing this:

## `npm i -g firebase-tools`

Login to your umwad portal Gmail account and do the following:

## `firebase deploy`
